[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# Redirect all requests to index.html for SPA routing (if needed)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200

# Security headers are already in static/_headers file
